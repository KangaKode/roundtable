# {{ project_name }} - Kubernetes Service
# Generated by aiscaffold v3

apiVersion: v1
kind: Service
metadata:
  name: {{ project_slug }}
  labels:
    app: {{ project_slug }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ project_slug }}
